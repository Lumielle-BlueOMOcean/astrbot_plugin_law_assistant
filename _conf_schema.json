{
  "operator_ids": {
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "description": "除 AstrBot Admin 外，可以私聊控制 Law Assistant 的 QQ 用户 ID。"
  },
  "timezone": {
    "type": "string",
    "default": "Asia/Shanghai",
    "description": "活动时间和调度使用的 IANA 时区。"
  },
  "auto_scan_enabled": {
    "type": "bool",
    "default": false,
    "description": "是否启用自动扫描和提醒；默认关闭。"
  },
  "scan_interval_minutes": {
    "type": "int",
    "default": 60,
    "minimum": 5,
    "maximum": 1440,
    "description": "自动扫描间隔，单位为分钟。"
  },
  "llm_provider_id": {
    "type": "string",
    "default": "",
    "description": "可选的 LLM provider ID；留空时使用当前会话或宿主默认 provider。"
  },
  "extra_event_source_urls": {
    "type": "list",
    "items": {"type": "string"},
    "default": [],
    "description": "可选的同主机通知列表页 URL。"
  },
  "auto_publish_events": {
    "type": "bool",
    "default": false,
    "description": "是否将新发现的活动自动发送到已绑定目标；须由管理员预授权。"
  },
  "deadline_reminder_days": {
    "type": "list",
    "items": {"type": "int"},
    "default": [7, 3, 1],
    "description": "DDL 提前提醒天数。"
  },
  "deadline_same_day_enabled": {
    "type": "bool",
    "default": true,
    "description": "是否在 DDL 当日提醒。"
  },
  "daily_case_enabled": {
    "type": "bool",
    "default": false,
    "description": "是否自动发布每日经典案例；需要可用 LLM provider。"
  },
  "daily_case_time": {
    "type": "string",
    "default": "08:00",
    "description": "每日案例发布时间（本地时区 HH:MM）。"
  },
  "daily_case_selection_mode": {
    "type": "string",
    "default": "random",
    "description": "每日案例方向模式：random、fixed 或 rotation。"
  },
  "daily_case_subject": {
    "type": "string",
    "default": "",
    "description": "每日案例固定方向；留空表示随机或使用轮换列表。"
  },
  "daily_case_rotation_subjects": {
    "type": "list",
    "items": {"type": "string"},
    "default": [],
    "description": "每日案例轮换方向的有序列表。"
  },
  "daily_case_rotation_start_date": {
    "type": "string",
    "default": "",
    "description": "每日案例轮换起始日期 YYYY-MM-DD。"
  },
  "daily_case_rotation_start_index": {
    "type": "int",
    "default": 0,
    "minimum": 0,
    "description": "每日案例轮换的起始位置。"
  },
  "daily_question_enabled": {
    "type": "bool",
    "default": false,
    "description": "是否自动发布每日法考风格练习题；需要可用 LLM provider。"
  },
  "daily_question_time": {
    "type": "string",
    "default": "08:00",
    "description": "每日练习题发布时间（本地时区 HH:MM）。"
  },
  "daily_question_selection_mode": {
    "type": "string",
    "default": "random",
    "description": "每日一题方向模式：random、fixed 或 rotation。"
  },
  "daily_question_origin": {
    "type": "string",
    "default": "random",
    "description": "每日一题来源：real、mock 或 random。真题必须来自已核验题库。"
  },
  "daily_question_subject": {
    "type": "string",
    "default": "",
    "description": "每日一题固定方向；留空表示随机或使用轮换列表。"
  },
  "daily_question_type": {
    "type": "string",
    "default": "",
    "description": "每日一题题型；留空表示随机。"
  },
  "daily_question_rotation_subjects": {
    "type": "list",
    "items": {"type": "string"},
    "default": [],
    "description": "每日一题轮换方向的有序列表。"
  },
  "daily_question_rotation_start_date": {
    "type": "string",
    "default": "",
    "description": "每日一题轮换起始日期 YYYY-MM-DD。"
  },
  "daily_question_rotation_start_index": {
    "type": "int",
    "default": 0,
    "minimum": 0,
    "description": "每日一题轮换的起始位置。"
  },
  "case_source_court_enabled": {
    "type": "bool",
    "default": true,
    "description": "是否启用最高人民法院案例列表来源。"
  },
  "case_source_spp_enabled": {
    "type": "bool",
    "default": true,
    "description": "是否启用最高人民检察院案例列表来源。"
  },
  "law_update_enabled": {
    "type": "bool",
    "default": false,
    "description": "是否启用法律法规更新来源。"
  },
  "http_timeout_seconds": {
    "type": "int",
    "default": 20,
    "minimum": 5,
    "maximum": 120,
    "description": "单次异步 HTTP 请求超时秒数。"
  }
}
